#include <stdio.h>
#include <math.h>

int main() {
    double raio, altura, volume;
    
    printf("Digite o raio da base e a altura do cone: ");
    scanf("%lf %lf", &raio, &altura);
    
    volume = (1.0 / 3.0) * M_PI *
