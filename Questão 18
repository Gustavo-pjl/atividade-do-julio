#include <stdio.h>

int main() {
    double peso, altura, imc;
    
    printf("Digite o seu peso em quilogramas e altura em metros: ");
    scanf("%lf %lf", &peso, &altura);
    
    imc = peso / (altura * altura);
    
    printf("O IMC é: %.2lf\n", imc);
    
    return 0;
}
